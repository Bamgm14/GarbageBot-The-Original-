#Offline Command Mode
import os
import Commands as c
import datetime as d
class Node:
    def send_keys(x):
        if os.path.isfile(x):
            os.system('"'+x+'" && echo Hello')
        return None
    def click():
        return None
class Nod(Node):
    def find_element_by_xpath(var=None):
        return Node
message=''
while message!='bye garb':
    message=input("Input Commmand:")
    c.Commands(Nod,Node,date=d.datetime.now().isoformat(),message=message)
